<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base application theme. -->
    <!-- inherit from the material theme -->
    <style name="AppTheme" parent="android:Theme.Material">
        <!--
            Theme customizations available in newer API levels can go in
            res/values-vXX/styles.xml, while customizations related to
            backward-compatibility can go here.
        -->
        <!-- Main theme colors -->
        <!--   your app branding color for the app bar -->
        <item name="android:colorPrimary">@color/colorPrimary</item>
        <!--   darker variant for the status bar and contextual app bars -->
        <item name="android:colorPrimaryDark">@color/colorPrimaryDark</item>
        <!--   theme UI controls like checkboxes and text fields -->
        <item name="android:colorAccent">@color/textColorPrimary</item>
        <item name="android:colorButtonNormal">@color/buttonColor</item>
        <item name="android:elevation">1dp</item>

    </style>

    <style name="Widget.AppCompat.Button." parent="Base.TextAppearance.AppCompat.Button">
        <item name="android:background">@color/actionButtonColor</item>
    </style>


</resources>
